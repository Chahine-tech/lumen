# Terraform state — contains VM IDs and sensitive data, never commit
terraform.tfstate
terraform.tfstate.backup

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Rendered cloud-init files (generated by terraform apply, contain SSH key)
cloud-init/node1-rendered.yaml
cloud-init/node2-rendered.yaml
