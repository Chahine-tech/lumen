# Metrics Server v0.8.0 - Airgap Override Values
# Required for HPA (Horizontal Pod Autoscaler)

image:
  repository: localhost:5000/metrics-server/metrics-server
  tag: "v0.8.0"
  pullPolicy: IfNotPresent

# Required for K3s - override default args completely to avoid duplication
defaultArgs: false
args:
  - --cert-dir=/tmp
  - --secure-port=4443
  - --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname
  - --kubelet-use-node-status-port
  - --metric-resolution=15s
  - --kubelet-insecure-tls

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
